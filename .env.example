# ============================================================
# Dental Pricing Calculator - Environment Configuration
# ============================================================
# Copy this file to .env and update the values for your environment
# ============================================================

# ==================== SERVER CONFIGURATION ====================
# Network binding (0.0.0.0 allows external access)
HOST=0.0.0.0
PORT=5002

# Environment: development or production
# IMPORTANT: Set to 'production' in production to enable safety features
FLASK_ENV=development
ENVIRONMENT=development

# Secret key for session encryption (REQUIRED IN PRODUCTION!)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dental-calculator-secret-key-change-me

# ==================== DATABASE CONFIGURATION ====================
# Leave empty to use platform-appropriate default location
# In production, set this to a persistent location outside the app folder
# DATABASE_PATH=/var/data/dental_calculator.db

# Create sample data on startup (NEVER set True in production!)
# This is blocked in production mode regardless of this setting
CREATE_SAMPLE_DATA=False

# ==================== SESSION CONFIGURATION ====================
# Set to True only if using HTTPS
SESSION_COOKIE_SECURE=False

# ==================== EMAIL CONFIGURATION (SMTP) ====================
# Required for email verification and password reset functionality

# Enable/disable email sending (False = log emails to console instead)
MAIL_ENABLED=False

# SMTP Server Settings
# Common servers:
#   Gmail: smtp.gmail.com (port 587, TLS)
#   Outlook: smtp.office365.com (port 587, TLS)
#   Yahoo: smtp.mail.yahoo.com (port 587, TLS)
#   Custom: your SMTP server
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False

# SMTP Authentication
# For Gmail: You must use an App Password, not your regular password
#   1. Enable 2-Factor Authentication on your Google account
#   2. Generate App Password at: https://myaccount.google.com/apppasswords
#   3. Use that 16-character password below
MAIL_USERNAME=mohamedelgohary47@gmail.com
MAIL_PASSWORD=geee jyer qzjg wxke

# Default sender shown in emails (format: "Name <email@example.com>")
MAIL_DEFAULT_SENDER=Dental Pricing Calculator <mohamedelgohary47@gmail.com>

# Debug mode - logs SMTP communication (set False in production)
MAIL_DEBUG=False

# ==================== APPLICATION URLS ====================
# Frontend URL used in email links (verification, password reset)
# Development: http://localhost:5002
# Production: https://your-domain.com
FRONTEND_URL=http://localhost:5002

# ==================== TOKEN EXPIRATION ====================
# Email verification token expiry (in hours)
EMAIL_VERIFICATION_EXPIRY_HOURS=24

# Password reset token expiry (in hours)
PASSWORD_RESET_EXPIRY_HOURS=1

# ============================================================
# PRODUCTION CHECKLIST
# ============================================================
# Before deploying to production, ensure:
# 1. FLASK_ENV=production
# 2. ENVIRONMENT=production
# 3. SECRET_KEY is a unique, random 64-character hex string
# 4. DATABASE_PATH points to a persistent location (backed up regularly)
# 5. CREATE_SAMPLE_DATA=False (blocked in production anyway)
# 6. SESSION_COOKIE_SECURE=True (if using HTTPS)
# 7. MAIL_ENABLED=True with valid SMTP credentials
# 8. FRONTEND_URL set to your production domain
# ============================================================
