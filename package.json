{
  "name": "dental-pricing-calculator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd .. && mkdir -p static && cp -r frontend/dist static/",
    "vercel-build": "cd frontend && npm ci && npm run build && cd .. && mkdir -p static && cp -r frontend/dist static/",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
